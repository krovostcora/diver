{
  "name": "diver",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "lint": "echo \"No lint configured\"",
    "check": "vite build --emptyOutDir",
    "smoke": "wait-on http://localhost:4173 && curl -sSf http://localhost:4173 >/dev/null"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "cross-port-killer": "^1.4.0",
    "pixi.js": "^8.14.0",
    "vite": "^5.0.0",
    "wait-on": "^9.0.3"
  }
}
